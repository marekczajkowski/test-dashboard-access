server {
    listen      80 default_server;
    server_name _;

    location /health {
            access_log off;
            add_header  Content-Type    text/html;
            return 200;
    }

    location /nginx_status {
        stub_status;
        allow 127.0.0.1;
        deny all;
    }
}